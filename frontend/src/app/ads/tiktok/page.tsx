"use client";

import { AdsAssistantPage } from "@/components/ads/ads-assistant-page";

/** Trợ lý quảng cáo — TikTok Shop (khung preview, chờ Marketing API). */
export default function TiktokAdsPage() {
  return <AdsAssistantPage platform="TIKTOK" />;
}
