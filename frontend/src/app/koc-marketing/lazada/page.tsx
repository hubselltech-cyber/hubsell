"use client";

import { KocChannelPage } from "@/components/koc/koc-channel-page";

/** Lazada Affiliate — số thật từ sao kê Finance API của 2 gian đã liên kết. */
export default function KocLazadaPage() {
  return <KocChannelPage platform="LAZADA" />;
}
