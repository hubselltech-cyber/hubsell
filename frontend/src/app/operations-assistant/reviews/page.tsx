"use client";

import { OperationsReviewsPage } from "@/components/operations/reviews-page";

/** Trợ lý vận hành — Phản hồi đánh giá (khung preview, chờ API sàn). */
export default function ReviewsPage() {
  return <OperationsReviewsPage />;
}
