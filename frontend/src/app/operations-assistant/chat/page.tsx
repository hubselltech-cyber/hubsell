"use client";

import { OperationsChatPage } from "@/components/operations/chat-page";

/** Trợ lý vận hành — Trợ lý Chat CSKH (khung preview, chờ API sàn). */
export default function ChatPage() {
  return <OperationsChatPage />;
}
